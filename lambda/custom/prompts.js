'use strict';

module.exports = {
    returnQuestionPrompts: function() {
        const promptArray = [
            "Is the statement true or false ? ",
            "Is the statement correct or incorrect ? " +
            "Yes or no ? "
        ];
        return promptArray;
    }
};